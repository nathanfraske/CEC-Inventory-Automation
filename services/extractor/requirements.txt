# Web layer for the extractor service. The ML/vision deps (vLLM/llama.cpp, opencv-python,
# invoice2data) are installed on the inference box per its hardware and are intentionally
# not pinned here — the deterministic template path needs only the stdlib.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2
