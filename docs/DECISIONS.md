# DECISIONS — locked choices and rationale

> Last updated: 2026-06-27 · Dating + tombstoning rules: `CLAUDE.md` §3.
> Each decision is dated and, where it resolves an open question from the scope, mapped to its
> `INV-OQ`. Decisions are "locked for this build, all flippable" (`AGENT_RUNBOOK.md` §0). When
> a decision is reversed, **tombstone** it (do not delete) and add the replacement.

## Active decisions

| # | Date | Decision | Resolves | Rationale |
|---|---|---|---|---|
| D-001 | 2026-06-27 | Backend = Rust + Axum; data access via SQLx. (Phase 0 uses runtime queries only; compile-time-checked `query!`/`query_as!` queries and the committed `.sqlx/` cache arrive in Phase 1.) | INV-OQ-1 | Leaner than SeaORM; fits the verification posture. |
| D-002 | 2026-06-27 | Database = PostgreSQL 16. | — | Relational fits inventory; JSONB for raw extract / event detail / carrier payloads. |
| D-003 | 2026-06-27 | Object storage = filesystem behind the API; MinIO is a drop-in later. | INV-OQ-2 | Simplest for one box; `STORAGE_BACKEND` flips it. |
| D-004 | 2026-06-27 | Frontend = Axum server-render + HTMX + small JS islands (camera/scan). Scaffold ships API + health spine only; UI is Phase 1+. | INV-OQ-3 | Least total code; camera/scan are the only places the choice bites. |
| D-005 | 2026-06-27 | Repo shape = Cargo workspace monorepo, crates `api` / `poller` / `domain`; Python extractor under `services/extractor`. | — | Matches the Rust-core / Python-isolated-to-ML split. |
| D-006 | 2026-06-27 | License = Apache-2.0. | — | Stated in the runbook; the existing repo `LICENSE` already is Apache-2.0. |
| D-007 | 2026-06-27 | Secret hygiene = `.gitignore`-first, real secrets only in the gitignored `.env` (from `scripts/gen_secrets.sh`); gitleaks in CI + a git pre-commit hook; DB data in a Docker named volume, dumps outside the repo. | — | `SECRETS-AND-DATABASE.md`. The repo is public-shaped; assume anything committed leaks. |
| D-008 | 2026-06-27 | Configuration hooks = git pre-commit secret-scan (`.githooks/pre-commit`, `core.hooksPath=.githooks`) + Claude Code SessionStart hook (`.claude/`). | — | The pre-commit hook is the optional gitleaks hook called for in `SECRETS-AND-DATABASE.md` §7; the SessionStart hook enforces hygiene and warms the build cache each session. |
| D-009 | 2026-06-27 | Memory-document model = `CLAUDE.md` + `docs/HANDOFF.md` + `docs/TODO.md` + `docs/DECISIONS.md` + `CHANGELOG.md`, governed by an explicit dating + tombstoning protocol (`CLAUDE.md` §3). | — | Durable, auditable state so any later agent/human resumes without guessing; mirrors the append-only `unit_event` ethos. |
| D-010 | 2026-06-27 | Phase 0 CRUD uses SQLx **runtime** queries (`query`/`query_as` + `.bind`), not the `query!` macros; the api crate is lib+bin so tests drive the real router; money is `rust_decimal::Decimal`; uploads use axum `multipart`. | INV-OQ-1 | Keeps the offline/CI build green with no `.sqlx/` to manage yet and matches the existing spine. The move to compile-time-checked queries + committed `.sqlx/` (and a Postgres service in CI to run the integration test) is the deliberate follow-up tracked in `docs/TODO.md`. |
| D-011 | 2026-06-27 | Shipment tracking lives in a new `crates/tracking` lib behind a `CarrierProvider` trait, shared by the API poll endpoint and the poller binary. Ships with `none` (no-op) + `mock` (deterministic) providers; real carriers/aggregator plug in later. | INV-OQ-30 | One poll engine, two callers; the trait keeps the single external dependency (carrier APIs) swappable and the system safe to run before a provider is chosen. |
| D-012 | 2026-06-27 | Auth = argon2 password hashes (`app_user`, migration 0002) + axum-extra **signed** session cookies keyed off `SESSION_SECRET`; `require_auth` middleware protects the data/mutation routes; `/health`, `/readyz`, `/auth/*`, read-only UI stay public. Behind the Headscale mesh. | §18 | A thin login for a few operators; signed cookies avoid server-side session storage. `app(state)` enables auth; `build_app(state, false)` (tests only) skips it. Cookie `Secure` is left off so it works over http on the mesh — enable under TLS. |
| D-013 | 2026-06-27 | The full stack runs from one `docker-compose.yml` (`db`+`extractor`+`api`+`poller`, healthchecked). In-network containers reach Postgres at host **`db`** and the extractor at **`extractor`**, so the Rust services build `DATABASE_URL` from the `POSTGRES_*` parts inside compose rather than reusing the `.env` `localhost` URL (which is for host-run dev). A `.dockerignore` keeps `.env`/keys/dumps out of image layers. A CI `compose` job brings the stack up with `--wait` and smoke-tests `/readyz` + the extractor `/health`. | — | One command stands up the integrated system; the host-split `DATABASE_URL` avoids the classic "works on host, can't resolve `localhost` in-container" trap; CI exercises the container path the dev sandbox can't (no daemon), closing gate E mechanically. |
| D-014 | 2026-06-27 | The operator UI stays server-rendered HTML (no template engine) and performs **all mutations by POSTing JSON to the existing auth-protected API routes**, carrying the browser's signed session cookie — not via separate form-encoded handlers. A single client helper (`cecSubmit`) serializes a form's fields to JSON (honoring `data-type` number/bool/lines) and renders the response inline. UI pages render publicly; their actions require a logged-in session. | INV-OQ-3 / §18 | One code path per operation (the JSON API is the single contract); no duplicate form-decoding handlers to keep in sync; auth is reused for free. The UI is a thin presentation layer over the API the cec.direct seam also uses. |
| D-018 | 2026-06-27 | External/machine-to-machine apps authenticate with **service-account API tokens** (`Authorization: Bearer cec_pat_…`), not cookie sessions. Tokens are stored only as a `sha256` hash (`api_token`, migration 0005), carry a role (`operator`/`admin`), and are minted/listed/revoked by admins (`/auth/tokens`). One `resolve_role` path backs both cookie and bearer auth across `require_auth`/`require_admin`. Integration contract: `docs/INTEGRATION.md`. | §18 / §19 | Cookie-session login is clunky for servers; a bearer token is the standard m2m pattern and lets the cec.direct seam (and any external app) integrate cleanly. SHA-256 (not argon2) because tokens are high-entropy and verified per-request. Fine-grained per-endpoint scopes remain a follow-up. |
| D-017 | 2026-06-27 | **Serial numbers are globally unique** (owner decision). Migration 0003 replaces the plain `unit_serial_idx` with a partial unique index on `inventory_unit.serial_number WHERE serial_number IS NOT NULL` (many NULLs still allowed); duplicate binds/intakes return 409 via the central unique-violation mapping. Bulk-stock `asset_tag` is also made `UNIQUE` (units/systems already were). The audit/event tables (`unit_event`, `system_validation`, `system_transfer`, `shipment_event`) get a `BEFORE UPDATE OR DELETE` trigger enforcing append-only at the DB level. | §13 / §16 / audit | The owner chose global uniqueness (vs per-product or allow-dupes); sweep/proof/warranty logic all assume a serial maps to one unit. Append-only-by-trigger makes the integrity backbone tamper-evident even against a stray query or a cascade delete. Pre-existing duplicates must be deduped before 0003 applies. | 
| D-016 | 2026-06-27 | Security hardening from the audit panels (`docs/AUDIT-2026-06-27.md`): `SESSION_SECRET` **fails closed** (no baked default, no zero-pad, ≥64 bytes); a global 1 MiB request body cap with a 25 MiB override on upload routes; state-changing reserve/consume and event-logging paths are transactional with the row lock **inside** the tx; operator-supplied `serial_format_regex` is compiled with a size limit; extractor/`from-payload` line items are validated + size-capped; password floor is 12; login is constant-time for unknown users; backups now cover the object store too. | §18 / audit | The old `SESSION_SECRET` fallback was a forgeable known key; the rest close DoS, concurrency, and injection gaps the panels found. CI `tests` supplies a throwaway `SESSION_SECRET` since build_state now requires it. Larger items (RBAC, rate-limiting, session revocation, serial uniqueness, backup automation/encryption, non-root containers) are the tracked backlog. |
| D-019 | 2026-06-27 | The api service's **published host port** is configurable via `API_PUBLISH_PORT` (default 8080); the container-internal port stays 8080. | — | On this compute box the `cec-llm-broker` permanently owns `127.0.0.1:8080`, so the api publishes on `8081`. Keeps the committed default unchanged for other environments; the box-specific value lives in the gitignored `.env`. |
| D-022 | 2026-06-27 | Backup/restore scripts are **container-aware**: when the host lacks `psql`/`pg_dump`/`pg_restore` they route through the `db` compose service, and they archive/restore receipts via the `<project>_objects` named volume when no host `STORAGE_FS_ROOT` exists (`scripts/_pglib.sh`, `CEC_PG_MODE`). Backups stay age-encrypted; the key lives outside the repo. | §7 / audit (backups) | The target box runs Postgres + the object store in Docker and may have no host pg client tools (and no passwordless sudo to add them); routing through the container makes backups work wherever the stack runs, with host tools still preferred when available. |
| D-021 | 2026-06-27 | Receipt-image extraction is **asynchronous**: `POST /purchases/from-image-async` registers an in-memory job (pruned 30 min) and returns 202 immediately; a background task resolves warm-state then runs extract+persist; the UI polls `model_warm`+`status` to show 'warming' vs 'extracting' before opening the draft to confirm. The vision seat is **`cec-vision-judge`** (Qwen3-VL-32B), kept resident via `scripts/vlm_keepwarm.sh` + a systemd timer. api→extractor calls use bounded timeouts. Extends D-020. | §11.2 / §18 | A cold VLM load is too slow to hold an HTTP request open; the async job + warm-status give the operator a responsive, honest UI. cec-vision-judge is GPU-resident (faster warm OCR than the CPU-offloaded 35B seat). | D-020 |
| D-020 | 2026-06-27 | Receipt-**image** vision adds an `openai` backend (`EXTRACTOR_VLM_BACKEND=openai`): an OpenAI-compatible `/chat/completions` call with a data-URI `image_url` block, aimed by `EXTRACTOR_VLM_BASE_URL` at the on-box `cec-llm-broker` (model e.g. `cec-worker-vision`). Committed default stays `stub`. Extends D-015. | §11.2 | The local broker is OpenAI-shaped (not Anthropic Messages), and on-box inference keeps receipt images on-prem (the §11.2 goal). Live-validated against Qwen3-VL on the 5090. |
| D-015 | 2026-06-27 | The receipt-**image** path is backend-pluggable (`EXTRACTOR_VLM_BACKEND`): `stub` (default) or `claude` (interim — the Anthropic Messages API, a hosted Claude vision model) until the local VLM on the inference box is wired. The API also exposes `POST /purchases/from-payload`, which persists a **caller-supplied** §11.4 extraction object, so any external extractor — including an operator or in-the-loop agent vision pass — can feed the receipt→inventory loop without the Python service. The `claude` backend is opt-in/off-by-default and key-from-env. | §11.2 / §3 | Unblocks image receipts now without GPU hardware; `from-payload` decouples the loop from any single extractor (the §11.4 JSON is the contract). The default-stub + env-key keeps the build hermetic and avoids third-party image egress unless explicitly enabled. |
| D-023 | 2026-06-27 | Extractor money crosses the §11.4 seam as **decimal strings**, not JSON numbers. Both extractor paths emit 2-decimal strings (`extractor._money_str`); `extractor.rs::money()` parses a string straight to `Decimal` (numeric(12,2)) and keeps a numeric fallback for back-compat with `from-payload` callers that still send numbers. | §11.4 / audit | IEEE-754 floats drift against `numeric(12,2)` (e.g. a parsed 1099.10 → 1099.0999…), corrupting receipt totals — the shop's #1 accuracy priority. Strings are exact and human-auditable; the numeric fallback keeps existing callers working. Validated end-to-end through the broker. | D-015 |

## Deviations recorded this build (sandbox-only, do not propagate to the target box)

| # | Date | Deviation | Why | Correct path on a real box |
|---|---|---|---|---|
| V-001 | 2026-06-27 | Postgres started as a **native local cluster** instead of `docker compose up -d db`. | The docker daemon is not reachable in the web sandbox. Same user/db/port/version. | `docker compose up -d db` (the documented, committed path). The CI `compose` job now exercises the real `docker compose up --build` path on the GitHub runner each push (gate E), so the container path is no longer unverified — only the local dev box substitutes. **✅ RESOLVED [2026-06-27]** — `docker compose up -d --build` brought the full stack up healthy on the compute box; gate E closed. |
| V-002 | 2026-06-27 | Gate F secret scan done **manually**; gitleaks not run locally. | Installing the gitleaks binary is denied in this environment. | `gitleaks detect --source . --redact` locally; CI runs it on push regardless. **✅ RESOLVED [2026-06-27]** — gitleaks 8.30.1 installed on the box; local history scan clean (no leaks); gate F closed. |

> The deviations above are environment workarounds, not design decisions. The committed repo
> still describes and uses the docker + gitleaks paths exactly as written.

## Tombstoned / reversed decisions

_None yet._
